# ==========================================
# Core Configuration
# ==========================================
# Environment Mode: production, development, testing
VECTORDB_ENV=development
# Debug Log Level
VECTORDB_DEBUG=0

# ==========================================
# Server Settings
# ==========================================
VECTORDB_HOST=0.0.0.0
VECTORDB_PORT=8000

# API Authorization Key (Admin routes)
# STRONGLY RECOMMENDED: Change this to a strong random string in production!
VECTOR_API_KEY=dev-key-please-change

# ==========================================
# Database Connections
# ==========================================
# Qdrant Vector Database
QDRANT_URL=http://localhost:6333
# QDRANT_API_KEY=your-qdrant-api-key

# Redis (Cache, Celery Broker, Lock)
REDIS_URL=redis://localhost:6379/0

# ==========================================
# Model Paths & Settings
# ==========================================
# Dense Embedding Model (HuggingFace ID or Local Path)
VECTOR_MODEL_PATH=BAAI/bge-m3

# Sparse Model (SPLADE)
SPLADE_MODEL_NAME=yjoonjang/splade-ko-v1
SPLADE_DEVICE=cpu
SPLADE_MAX_LENGTH=256

# Reranker Model (Cross-Encoder)
RERANKER_MODEL_NAME=BAAI/bge-reranker-v2-m3

# ==========================================
# Feature Flags
# ==========================================
# Enable Secure Logging (HMAC integrity checks)
SECURE_LOG=true
# Key for HMAC signing logs (Change this!)
LOG_KEY=change-this-secret-log-key
